
header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                <i class="fab fa-youtube"></i>

        .header__column
            form(action=routes.search, method="get")
                input(type="text", placeholder="search by items", name="term")

        .header__column
            ul
                //app.js에 fake user 있음(res.locals로)
                //user 정보 저장 후 header change
                if !loggedUser
                    li
                        a(href=routes.join) Join
                    li
                        a(href=routes.login) Log In
                else
                    li
                        a(href=`/videos${routes.upload}`) Upload
                    li
                        a(href=routes.me) User Detail
                    li
                        a(href=routes.logout) Log Out